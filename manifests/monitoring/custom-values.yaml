grafana:
  grafana.ini:
    server:
      domain: ""
      root_url: "%(protocol)s://%(domain)s:%(http_port)s/grafana/"
      serve_from_sub_path: true

prometheus:
  prometheusSpec:
    web:
      routePrefix: /prometheus

