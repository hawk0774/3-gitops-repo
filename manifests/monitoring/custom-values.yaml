grafana:
  service:
    type: ClusterIP
    port: 80
  grafana.ini:
    server:
      domain: localhost
      root_url: "%(protocol)s://%(domain)s/grafana/"
      serve_from_sub_path: true  

prometheus:
  prometheusSpec:
    web:
      routePrefix: /prometheus

